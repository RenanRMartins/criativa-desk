DATABASE_URL=postgresql://user:password@localhost:5432/criativa_desk
JWT_SECRET=change-this-to-a-strong-random-secret
ANTHROPIC_API_KEY=sk-ant-...
SOCIAL_MOCK_MODE=true
FRONTEND_URL=http://localhost:3000
# Origens extras aceitas no CORS (domínio antigo durante uma troca), separadas por vírgula
# EXTRA_CORS_ORIGINS=https://criativa-desk.vercel.app

# Meta: IDs das configurações do Login para Empresas (painel → Facebook Login for
# Business → Configurations). Ausentes, o fluxo usa o scope tradicional.
# META_CONFIG_FACEBOOK=
# META_CONFIG_INSTAGRAM=
